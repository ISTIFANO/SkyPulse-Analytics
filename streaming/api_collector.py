# import requests

# FAKE_API_URL = "http://localhost:9000/batch"

# def get_tweets(batch_size=10):
#     response = requests.get(FAKE_API_URL, params={"batch_size": batch_size})
#     if response.status_code == 200:
#         return response.json()
#     return []
